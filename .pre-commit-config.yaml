repos:
  - repo: local
    hooks:
      - id: qa-full
        name: Run full QA suite
        entry: make qa/full
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]
